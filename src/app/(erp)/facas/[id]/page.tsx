/**
 * Rota necessária para o Next.js reconhecer URLs como /facas/{uuid}.
 * O conteúdo é renderizado por ErpTabs (layout do ERP não usa {children}).
 */
export default function FacaDetalhePage() {
  return null
}
